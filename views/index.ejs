<!DOCTYPE html>
<html>
<head>
  <title>CRUD App</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Jost:wght@300;400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" type="text/css" href="/styles.css">
</head>
<body>
  <div class="container mt-4">
    <div class="jumbotron img1">
      <h1 class="display-4 text-capitalize font-weight-bold bg-success p-2 jumbotron d-inline">Memory App</h1>
      <p class="lead mt-4 "><mark class="bg-warning rounded p-2 h6">"Manage your tasks effortlessly with a touch of memory."</mark> </p>
    </div>

    <!-- Add Item Form -->
   <!-- Add Item Form -->
<form method="POST" action="/" class="mb-4">
    <div class="input-group">
      <input type="text" class="form-control" name="name" placeholder="Remember this..." required>
      <div class="input-group-append">
        <button type="submit" class="btn btn-primary px-4">
        Add to Memory  <i class="mx-2 fas fa-plus"></i>
        </button>
      </div>
    </div>
  </form>
  

    <!-- Item List -->
    <ul class="list-group">
      <% items.forEach(item => { %>
        <li class="list-group-item d-flex justify-content-between align-items-center">
          <%= item.name %>
          <form method="POST" action="/delete">
            <input type="hidden" name="itemId" value="<%= item._id %>">
            <button type="submit" class="btn btn-danger btn-sm">
                <i class="fas fa-trash-alt"></i>
              </button>
              
          </form>
        </li>
      <% }); %>
    </ul>
  </div>
</body>
</html>
